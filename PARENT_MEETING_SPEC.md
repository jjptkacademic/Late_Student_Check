# 📋 ระบบบันทึกการพบผู้ปกครอง - Specification Document

**โปรเจค:** Late Student Check System
**วันที่สร้าง:** 29 มกราคม 2026
**เวอร์ชัน:** 1.0

---

## 📌 Table of Contents

1. [Feature Overview](#feature-overview)
2. [Specification Summary](#specification-summary)
3. [Database Schema](#database-schema)
4. [API Endpoints](#api-endpoints)
5. [UI Design](#ui-design)
6. [Logic Flow](#logic-flow)
7. [File Structure](#file-structure)
8. [Implementation Checklist](#implementation-checklist)

---

## 🎯 Feature Overview

### วัตถุประสงค์
เพิ่มระบบบันทึกการพบผู้ปกครองของนักเรียนที่มาสาย เพื่อติดตามว่าครูได้พบผู้ปกครองแล้วหรือยัง

### Use Case
- ครูสามารถบันทึกว่าได้พบผู้ปกครองของนักเรียนที่มาสายบ่อยแล้ว
- ระบบแสดง indicator (🟢/🔴) ว่าพบแล้วหรือยังในเดือนนี้
- สามารถดูประวัติการพบผู้ปกครองย้อนหลังได้
- สามารถลบการบันทึกที่ผิดพลาดได้

---

## 📊 Specification Summary

| รายการ | รายละเอียด |
|--------|-----------|
| **เดือนนี้** | เดือนปัจจุบัน (1-31 ของเดือนนั้นๆ) |
| **บันทึกชื่อครู** | ✅ มี (optional - ไม่ใส่ก็ได้) |
| **แสดง Records** | แสดงทุกครั้งที่พบในเดือนนั้น |
| **หมายเหตุ** | ❌ ไม่มี (แค่วัน/เวลา/ชื่อครู) |
| **UI Style** | แบบละเอียด (แสดงทุก record + ปุ่ม X ลบ) |
| **ตำแหน่งปุ่ม** | 2 ที่: ใน Modal + ในตาราง Ranking |

---

## 🗄️ Database Schema

### Table: `parent_meetings`

```sql
CREATE TABLE parent_meetings (
    meeting_id INT AUTO_INCREMENT PRIMARY KEY COMMENT 'รหัสการพบ (auto)',
    student_id INT NOT NULL COMMENT 'รหัสนักเรียน (FK)',
    meeting_date DATE NOT NULL COMMENT 'วันที่พบผู้ปกครอง',
    meeting_time TIME NOT NULL COMMENT 'เวลาที่พบผู้ปกครอง',
    teacher_name VARCHAR(100) DEFAULT NULL COMMENT 'ชื่อครูผู้พบ (optional)',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT 'วันเวลาที่บันทึก',

    FOREIGN KEY (student_id) REFERENCES students(student_id) ON DELETE CASCADE,

    INDEX idx_student_id (student_id),
    INDEX idx_meeting_date (meeting_date),
    INDEX idx_student_date (student_id, meeting_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='ตารางบันทึกการพบผู้ปกครอง';
```

### ตัวอย่างข้อมูล

```
meeting_id | student_id | meeting_date | meeting_time | teacher_name | created_at
-----------+------------+--------------+--------------+--------------+---------------------
1          | 15         | 2026-01-15   | 14:30:00     | ครูสมชาย     | 2026-01-15 14:35:00
2          | 15         | 2026-01-10   | 10:15:00     | ครูสมหญิง   | 2026-01-10 10:20:00
3          | 23         | 2026-01-20   | 09:00:00     | NULL         | 2026-01-20 09:05:00
```

### Database Relationships

```
students (1) ──────── (Many) late_records
    │
    │
    └──────────────── (Many) parent_meetings

ความสัมพันธ์:
- 1 นักเรียน มี หลาย late_records
- 1 นักเรียน มี หลาย parent_meetings
- ลบนักเรียน → ลบ records ทั้งหมด (CASCADE)
```

---

## 🔌 API Endpoints

### 1. GET `/api/parent-meetings`

**คำอธิบาย:** ดึงประวัติการพบผู้ปกครอง

**Query Parameters:**
- `student_id` (required): รหัสนักเรียน
- `month` (optional): เดือน (1-12) - default: เดือนปัจจุบัน
- `year` (optional): ปี (2026) - default: ปีปัจจุบัน

**Request Example:**
```
GET /api/parent-meetings?student_id=15&month=1&year=2026
```

**Response Success:**
```json
{
  "success": true,
  "data": [
    {
      "meeting_id": 1,
      "student_id": 15,
      "meeting_date": "2026-01-15",
      "meeting_time": "14:30:00",
      "teacher_name": "ครูสมชาย",
      "created_at": "2026-01-15 14:35:00"
    },
    {
      "meeting_id": 2,
      "student_id": 15,
      "meeting_date": "2026-01-10",
      "meeting_time": "10:15:00",
      "teacher_name": "ครูสมหญิง",
      "created_at": "2026-01-10 10:20:00"
    }
  ],
  "has_met_this_month": true,
  "total_meetings": 2
}
```

---

### 2. POST `/api/parent-meeting`

**คำอธิบาย:** บันทึกการพบผู้ปกครอง

**Request Body:**
```json
{
  "student_id": 15,
  "meeting_date": "2026-01-29",
  "meeting_time": "14:30:00",
  "teacher_name": "ครูสมชาย"
}
```

**Field Details:**
- `student_id` (required): รหัสนักเรียน
- `meeting_date` (optional): วันที่พบ - default: today
- `meeting_time` (optional): เวลาที่พบ - default: now
- `teacher_name` (optional): ชื่อครูผู้พบ

**Response Success:**
```json
{
  "success": true,
  "data": {
    "meeting_id": 3,
    "student_id": 15,
    "meeting_date": "2026-01-29",
    "meeting_time": "14:30:00",
    "teacher_name": "ครูสมชาย",
    "created_at": "2026-01-29 14:30:00"
  },
  "message": "บันทึกการพบผู้ปกครองเรียบร้อย"
}
```

---

### 3. DELETE `/api/parent-meeting/{meeting_id}`

**คำอธิบาย:** ลบการบันทึก (กรณีบันทึกผิด)

**Request Example:**
```
DELETE /api/parent-meeting/3
```

**Response Success:**
```json
{
  "success": true,
  "message": "ลบการบันทึกเรียบร้อย"
}
```

**Response Error:**
```json
{
  "success": false,
  "error": "ไม่พบการบันทึกนี้"
}
```

---

### 4. GET `/api/late-summary` (แก้ไข)

**คำอธิบาย:** เพิ่ม field `has_met_this_month`

**Response Success:**
```json
{
  "success": true,
  "data": [
    {
      "student_id": 15,
      "student_code": "13015",
      "first_name": "กรณ์ศุพัชร์",
      "last_name": "กาทอง",
      "class_room": "4/2",
      "grade_level": "4",
      "total_late": 5,
      "has_met_this_month": true
    }
  ]
}
```

---

## 🎨 UI Design

### 1. หน้า stats.html - Ranking Table

**เพิ่มคอลัมน์ "พบผู้ปกครอง":**

```
┌──────┬───────────┬──────────────────┬─────┬────────┬──────────┐
│ ลำดับ│ รหัส      │ ชื่อ-นามสกุล      │ ชั้น│ มาสาย  │ พบผปค    │
├──────┼───────────┼──────────────────┼─────┼────────┼──────────┤
│  1   │ 13015     │ กรณ์ศุพัชร์ กาทอง│ 4/2 │ 5 ครั้ง│ 🟢 [พบ]  │
│  2   │ 13022     │ สมชาย ใจดี       │ 4/1 │ 4 ครั้ง│ 🔴 [พบ]  │
│  3   │ 13033     │ สมหญิง รักเรียน  │ 4/3 │ 3 ครั้ง│ 🟢 [พบ]  │
└──────┴───────────┴──────────────────┴─────┴────────┴──────────┘
```

**HTML Structure:**
```html
<td class="text-center">
    <div class="parent-meeting-status">
        <span class="status-indicator ${hasMetThisMonth ? 'met' : 'not-met'}"></span>
        <button class="btn-small btn-meeting" onclick="showParentMeetingModal(student)">
            พบ
        </button>
    </div>
</td>
```

**CSS:**
```css
.status-indicator {
    display: inline-block;
    width: 10px;
    height: 10px;
    border-radius: 50%;
    margin-right: 5px;
}

.status-indicator.met {
    background-color: #4CAF50; /* เขียว */
}

.status-indicator.not-met {
    background-color: #f44336; /* แดง */
}
```

---

### 2. Modal รายละเอียดนักเรียน (แบบละเอียด)

```
┌─────────────────────────────────────────────────────┐
│ 👤 นักเรียน: กรณ์ศุพัชร์ กาทอง (13015)        [X]  │
│    ห้อง: 4/2                                        │
├─────────────────────────────────────────────────────┤
│                                                     │
│ 📊 สถิติการมาสาย: 5 ครั้ง                          │
│                                                     │
│ ┌─────────────────────────────────────────────────┐│
│ │ 📞 การพบผู้ปกครอง (เดือนนี้)                    ││
│ │                                                  ││
│ │ ┌──────────────────────────────────────────────┐││
│ │ │ 🟢 15 ม.ค. 2026 เวลา 14:30                 X│││
│ │ │    โดย: ครูสมชาย                             │││
│ │ └──────────────────────────────────────────────┘││
│ │ ┌──────────────────────────────────────────────┐││
│ │ │ 🟢 10 ม.ค. 2026 เวลา 10:15                 X│││
│ │ │    โดย: ครูสมหญิง                            │││
│ │ └──────────────────────────────────────────────┘││
│ │                                                  ││
│ │ [ 📅 บันทึกพบผู้ปกครอง ]                        ││
│ └─────────────────────────────────────────────────┘│
│                                                     │
│ ─────────────────────────────────────────────────  │
│                                                     │
│ 📅 ประวัติการมาสาย                                 │
│ ┌─────┬────────────┬─────────┬──────────────────┐ │
│ │  #  │ วันที่     │ เวลา    │ สาเหตุ           │ │
│ ├─────┼────────────┼─────────┼──────────────────┤ │
│ │  1  │ 10 ม.ค. 26│ 08:15   │ รถติด            │ │
│ │  2  │ 15 ม.ค. 26│ 08:20   │ ตื่นสาย          │ │
│ └─────┴────────────┴─────────┴──────────────────┘ │
│                                                     │
│                                      [ ปิด ]       │
└─────────────────────────────────────────────────────┘
```

---

### 3. Modal บันทึกการพบผู้ปกครอง

```
┌─────────────────────────────────────────┐
│ 📅 บันทึกการพบผู้ปกครอง           [X] │
├─────────────────────────────────────────┤
│                                         │
│ นักเรียน: กรณ์ศุพัชร์ กาทอง (13015)    │
│                                         │
│ วันที่:                                 │
│ ┌─────────────────────────────────────┐ │
│ │ 📅 29/01/2026     (วันนี้)         │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ เวลา:                                   │
│ ┌─────────────────────────────────────┐ │
│ │ ⏰ 14:30         (ตอนนี้)           │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ชื่อครูผู้พบ (ไม่ใส่ก็ได้):            │
│ ┌─────────────────────────────────────┐ │
│ │ ครูสมชาย                            │ │
│ └─────────────────────────────────────┘ │
│                                         │
│     [ ยกเลิก ]        [ บันทึก ✓ ]    │
└─────────────────────────────────────────┘
```

---

## 🔄 Logic Flow

### เมื่อเปิด Modal รายละเอียดนักเรียน

```javascript
async function showStudentDetail(student) {
    // 1. แสดง Modal
    openModal('detailModal');

    // 2. แสดงข้อมูลนักเรียน
    displayStudentInfo(student);

    // 3. โหลดประวัติการมาสาย
    const lateRecords = await API.getLateRecords({ student_id: student.student_id });
    displayLateRecords(lateRecords);

    // 4. โหลดประวัติการพบผู้ปกครอง (เดือนนี้)
    const meetings = await API.getParentMeetings(student.student_id);

    // 5. แสดง indicator และรายการ
    if (meetings.has_met_this_month) {
        showGreenIndicator(); // 🟢
        displayMeetingRecords(meetings.data);
    } else {
        showRedIndicator(); // 🔴
    }
}
```

---

### เมื่อกดปุ่ม "บันทึกพบผู้ปกครอง"

```javascript
async function recordParentMeeting(student) {
    // 1. เปิด Modal ฟอร์ม
    const modal = openModal('meetingFormModal');

    // 2. Set default values
    setDefaultDate(new Date());
    setDefaultTime(new Date());

    // 3. เมื่อกด "บันทึก"
    const formData = {
        student_id: student.student_id,
        meeting_date: getFormDate() || today,
        meeting_time: getFormTime() || now,
        teacher_name: getTeacherName() || null
    };

    // 4. เรียก API
    const result = await API.addParentMeeting(formData);

    if (result.success) {
        // 5. Refresh รายการ
        refreshParentMeetings(student.student_id);

        // 6. เปลี่ยน indicator เป็นสีเขียว
        updateIndicator('met');

        // 7. แสดง notification
        showNotification('✅ บันทึกการพบผู้ปกครองเรียบร้อย', 'success');

        // 8. ปิด Modal
        closeModal('meetingFormModal');
    }
}
```

---

### เมื่อกดปุ่ม X ลบ

```javascript
async function deleteMeetingRecord(meeting_id) {
    // 1. Confirm
    const confirmed = confirm('ต้องการลบการบันทึกนี้?');
    if (!confirmed) return;

    // 2. เรียก API
    const result = await API.deleteParentMeeting(meeting_id);

    if (result.success) {
        // 3. ลบ element จาก UI
        removeMeetingElement(meeting_id);

        // 4. เช็คว่ายังมี records เหลือไหม
        const remainingMeetings = await API.getParentMeetings(student_id);

        if (!remainingMeetings.has_met_this_month) {
            // 5. เปลี่ยน indicator เป็นสีแดง
            updateIndicator('not-met');
        }

        // 6. แสดง notification
        showNotification('✅ ลบการบันทึกเรียบร้อย', 'success');
    }
}
```

---

## 📂 File Structure

```
Late_Student_Check/
├── Backend-MySQL/
│   ├── schema.sql                    ← เพิ่มตาราง parent_meetings
│   ├── config.php                    ← Database config
│   ├── api.php                       ← Main API router
│   ├── endpoints/
│   │   ├── students.php
│   │   ├── late-records.php
│   │   └── parent-meetings.php       ← ใหม่!
│   └── migrate-from-sheets.php       ← Import data จาก Google Sheets
│
└── Frontend/
    ├── stats.html                     ← แก้ไข: เพิ่มคอลัมน์ + modal
    ├── js/
    │   ├── api-helper.js              ← เพิ่ม API functions
    │   ├── stats.js                   ← แก้ไข: เพิ่ม logic
    │   └── parent-meetings.js         ← ใหม่! (optional - แยก logic)
    └── css/
        └── stats.css                  ← แก้ไข: เพิ่ม style จุดสี
```

---

## ✅ Implementation Checklist

### Phase 1: Database Setup

- [ ] สร้างตาราง `parent_meetings` ใน MySQL
- [ ] ทดสอบ Foreign Key constraint
- [ ] ใส่ข้อมูลทดสอบ (dummy data)

### Phase 2: Backend API

- [ ] **GET** `/api/parent-meetings`
  - [ ] รับ parameter student_id, month, year
  - [ ] Query ข้อมูลจาก DB
  - [ ] คำนวณ has_met_this_month
  - [ ] Return JSON response

- [ ] **POST** `/api/parent-meeting`
  - [ ] รับ JSON body
  - [ ] Validate student_id exists
  - [ ] Set default date/time if not provided
  - [ ] Insert ลง DB
  - [ ] Return created record

- [ ] **DELETE** `/api/parent-meeting/{id}`
  - [ ] ตรวจสอบว่า meeting_id exists
  - [ ] Delete จาก DB
  - [ ] Return success/error

- [ ] **UPDATE** `/api/late-summary`
  - [ ] เพิ่ม JOIN กับ parent_meetings
  - [ ] เพิ่ม field has_met_this_month

### Phase 3: Frontend UI

#### stats.html
- [ ] เพิ่มคอลัมน์ "พบผู้ปกครอง" ใน Ranking Table
- [ ] เพิ่ม HTML structure สำหรับ status indicator (🟢/🔴)
- [ ] เพิ่มปุ่ม "พบ" ในแต่ละแถว
- [ ] แก้ไข Modal รายละเอียดนักเรียน:
  - [ ] เพิ่มส่วน "การพบผู้ปกครอง"
  - [ ] แสดงรายการ meetings พร้อมปุ่ม X
  - [ ] เพิ่มปุ่ม "บันทึกพบผู้ปกครอง"
- [ ] สร้าง Modal ฟอร์มบันทึกการพบผู้ปกครอง:
  - [ ] Input: วันที่ (default: today)
  - [ ] Input: เวลา (default: now)
  - [ ] Input: ชื่อครู (optional)
  - [ ] ปุ่ม ยกเลิก/บันทึก

#### CSS (stats.css)
- [ ] Style สำหรับ status indicator (.met, .not-met)
- [ ] Style สำหรับ meeting record list
- [ ] Style สำหรับ Modal ฟอร์มบันทึก
- [ ] Responsive design สำหรับ mobile

#### JavaScript (api-helper.js)
- [ ] เพิ่ม `API.getParentMeetings(student_id, month, year)`
- [ ] เพิ่ม `API.addParentMeeting(data)`
- [ ] เพิ่ม `API.deleteParentMeeting(meeting_id)`

#### JavaScript (stats.js)
- [ ] `loadParentMeetings(student_id)` - โหลดข้อมูล
- [ ] `displayMeetingRecords(meetings)` - แสดงรายการ
- [ ] `showParentMeetingModal(student)` - เปิด Modal ฟอร์ม
- [ ] `addParentMeeting(data)` - บันทึกข้อมูล
- [ ] `deleteMeetingRecord(meeting_id)` - ลบ record
- [ ] `updateIndicator(status)` - อัปเดตจุดสี
- [ ] `refreshParentMeetings(student_id)` - refresh หลังบันทึก/ลบ

### Phase 4: Testing

- [ ] ทดสอบ API ด้วย Postman/curl
- [ ] ทดสอบการบันทึก (วันนี้/กำหนดเอง)
- [ ] ทดสอบการลบ
- [ ] ทดสอบ indicator เปลี่ยนสี
- [ ] ทดสอบ edge cases:
  - [ ] นักเรียนที่ไม่มี record
  - [ ] นักเรียนมี multiple records
  - [ ] ข้ามเดือน (indicator ควรกลับเป็นแดง)
  - [ ] ชื่อครูไม่ใส่
- [ ] ทดสอบ responsive (mobile/tablet)

### Phase 5: Deployment

- [ ] Upload ไฟล์ PHP ขึ้น hosting
- [ ] Run schema.sql บน production DB
- [ ] Update config.php ด้วย credentials จริง
- [ ] Upload frontend files
- [ ] Test บน production environment
- [ ] Go live! 🚀

---

## 📝 Notes

### ข้อควรระวัง
1. **Timezone**: ตรวจสอบให้แน่ใจว่า MySQL timezone = Asia/Bangkok
2. **Validation**: ต้อง validate student_id ก่อน insert
3. **Error Handling**: จัดการ error ทุก edge case
4. **Security**: ใช้ prepared statements ป้องกัน SQL injection

### ข้อเสนอแนะเพิ่มเติม (Phase 2)
- [ ] เพิ่มฟีเจอร์ **"ส่งการแจ้งเตือน"** หากนักเรียนมาสายเกิน X ครั้ง แต่ยังไม่ได้พบผู้ปกครอง
- [ ] หน้า **Report** แยกต่างหาก สำหรับดูว่านักเรียนคนไหนควรนัดพบผู้ปกครอง
- [ ] Export รายงาน "นักเรียนที่ต้องติดตามพบผู้ปกครอง"
- [ ] สถิติ: จำนวนครั้งเฉลี่ยที่พบผู้ปกครองต่อเดือน

---

## 📞 Contact

หากมีคำถามหรือต้องการความช่วยเหลือ:
- เปิด Issue ใน GitHub
- ติดต่อทีมพัฒนา

---

**สร้างโดย:** Claude Sonnet 4.5
**วันที่:** 29 มกราคม 2026
**เวอร์ชัน:** 1.0

---

## 🎯 Quick Start Guide

### สำหรับ Developer

1. อ่าน Spec นี้ทั้งหมด
2. ทำตาม Checklist ทีละ Phase
3. Test หลังจบแต่ละ Phase
4. Deploy เมื่อเสร็จทั้งหมด

### สำหรับ Tester

1. ดู UI Design section
2. ทดสอบตาม Test cases ใน Phase 4
3. รายงาน bugs/issues

### สำหรับ User (ครู)

1. ดู UI Design เพื่อทำความคุ้นเคย
2. ทดสอบระบบเมื่อ Go live
3. ให้ Feedback

---

**หมายเหตุ:** เอกสารนี้อาจมีการอัปเดต กรุณาตรวจสอบเวอร์ชันล่าสุดเสมอ
